github protocols

on github
make new or use existing repo

terminal
cd to code/wdi
mkdir       //local folder
cd to that directory
and then touch file    //local file
git init
git status. nothing to commit
git add file
git commit -m "add file"
git remote -v. if nothing happens   // trouble shooting
git remote add origin /* copy repo link here */
git remote -v //should show a fetch and pull
git push origin master. error, then do:
git pull origin
git push origin master
git pull origin master
git push origin master
check github in that repo
